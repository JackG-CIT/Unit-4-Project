services:

  frontend:
    build: ./app
    depends_on:
      - payment
      - db
    environment:
      - DB_HOST=db
      - DB_NAME=goldeninvest
      - DB_USER=postgres
      - DB_PASSWORD=postgres
    volumes:
      - ./app:/app
      - ./app/static/uploads:/app/static/uploads

  # 👇 This is your ONE exposed frontend (for browser access)
  frontend_public:
    build: ./app
    ports:
      - "5000:5000"
    depends_on:
      - payment
      - db
    environment:
      - DB_HOST=db
      - DB_NAME=goldeninvest
      - DB_USER=postgres
      - DB_PASSWORD=postgres

  payment:
    build: ./payment
    ports:
      - "5001:5001"

  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_DB: goldeninvest
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: